devtypes directory contains configuration for each specific device type

Each device type directory contains files that are used as options in drop-down menus for example, some contain the "cipn" column in which is for defining elements that refer to the option.
